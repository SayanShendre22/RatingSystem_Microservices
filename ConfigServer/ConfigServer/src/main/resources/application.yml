server:
  port: 8085
  
  
  
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/SayanShendre22/Hotel-Rating-MicroServices-config
          clone-on-start: true

